//route to index.js inside a folder is same as folder name so here it is events as we are in events folder

export default function EventsPage() {
  return (
    <div>
      <h1>My Events</h1>
    </div>
  );
}
